package com.rama.mako.activities.settings

import com.rama.mako.activities.SettingsActivity

class SettingsExtController(private val activity: SettingsActivity) {
    fun setup() {
        // Base flavor: no extended features
    }
}
